(section
  (ref "jack")
  (title "Jack API Reference")
  (contents)

  (section
    (title "Subprogram map")
    (para "The following table shows the mapping between the Ada subprograms
      and the original C API.")
    (include "m_proc_map.ud")))
